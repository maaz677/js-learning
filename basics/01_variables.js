const accountId = 19123
let accountEmail = "killer@gmail.com"
var accountPassword = "12345"
accountCity = "lahore"
let accountState;

// accountId = 0979 // not allowed 

console.log(accountId);

/* 
var is not used due to errors instead use let
const cannot be changed
you can also not use variables as in line no 4 but is not recommended  
*/

console.table([accountId, accountEmail, accountPassword, accountCity, accountState])